[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Start with deterministic correctness checks suitable for the existing codebase.
# Additional style rules can be enabled incrementally without destabilizing CI.
select = ["E4", "E7", "E9", "F"]
